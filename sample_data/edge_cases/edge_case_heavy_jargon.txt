Syncing on the K8s cluster migration. The CI/CD pipeline in GitLab is failing on the e2e integration tests because the staging environment pod is OOM-killing. Decision: Bump the memory limit on the staging pods to 4Gi. Alex, update the helm charts and PR it by noon. Also, the ingress controller is throwing 502s intermittently. It's an issue with the ALB sticky sessions. Blocker: AWS support is taking forever to reply. Let's escalate the AWS ticket to sev-2. Jamie, ping our TAM and escalate the ticket immediately.